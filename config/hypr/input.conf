# Control your input devices
# See https://wiki.hypr.land/Configuring/Variables/#input
input {
  # Use multiple keyboard layouts and switch between them with Left Alt + Right Alt
  # kb_layout = us,dk,eu

  # Use a specific keyboard variant if needed (e.g. intl for international keyboards)
  # kb_variant = intl

  kb_options = compose:caps # ,grp:alts_toggle

  # Change speed of keyboard repeat
  repeat_rate = 40
  repeat_delay = 250

  # Start with numlock on by default
  numlock_by_default = true

  # Increase sensitivity for mouse/trackpad (default: 0)
  # sensitivity = 0.35

  # Turn off mouse acceleration (default: false)
  # force_no_accel = true

  touchpad {
    # Use natural (inverse) scrolling
    natural_scroll = true

    # Use two-finger clicks for right-click instead of lower-right corner
    clickfinger_behavior = true

    # Control the speed of your scrolling
    scroll_factor = 0.1

    # Enable the touchpad while typing
    disable_while_typing = true

    # Left-click-and-drag with three fingers
    # drag_3fg = 1
  }
}

# Scroll nicely in the terminal
windowrule = match:class (Alacritty|kitty), scroll_touchpad 1.5
windowrule = match:class com.mitchellh.ghostty, scroll_touchpad 0.2

# Enable touchpad gestures for changing workspaces
# See https://wiki.hyprland.org/Configuring/Gestures/
#gesture = 4, horizontal, workspace

# 3-finger gestures for window focus navigation
gesture = 4, right, dispatcher, exec, /home/lonnie/.config/hypr/scripts/scroll-prev-col
gesture = 4, left, dispatcher, exec, /home/lonnie/.config/hypr/scripts/scroll-next-col
#gesture = 3, up, dispatcher, movefocus, u
#gesture = 3, down, dispatcher, movefocus, d
gesture = 4, vertical, workspace
#gesture = 3, right, dispatcher, layoutmsg, focus left
#gesture = 3, left, dispatcher, layoutmsg, focus right
gesture = 3, up, dispatcher, sendshortcut, CTRL SHIFT, TAB, activewindow
gesture = 3, down, dispatcher, sendshortcut, CTRL, TAB, activewindow
gesture = 3, right, dispatcher, layoutmsg, colresize +2conf
gesture = 3, left, dispatcher, layoutmsg, colresize -conf
